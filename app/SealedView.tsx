"use client";
/* eslint-disable react-hooks/exhaustive-deps -- keyed effects synchronize remote sealed data and history */
import {useEffect,useMemo,useRef,useState} from "react";
import HistoryPanel,{movementTone} from "./HistoryPanel";
import {SegmentedView,SortableHeader,SortToolbar,type Direction} from "./MarketUI";
import SealedFilters from "./SealedFilters";
import {SignalBadge,StrictnessControl} from "./SignalControls";
import {marketSignal,type SignalSide,type SignalStrictness} from "./signal-utils";
import MultiSelectField from "./MultiSelectField";
import SaleScenario from "./SaleScenario";
import {parseSealedProducts} from "./domain/contracts";
import {formatPercent,formatUsd} from "./domain/formatters";
import type {PriceHistory,SealedGame,SealedProduct,SealedView as SealedViewMode} from "./domain/types";
import type {SealedQueryState} from "./state/market-query";
import {useCatalogPage} from "./data/useCatalogPage";
import {usePriceHistoryBatch} from "./data/usePriceHistoryBatch";
import MarketLeaderboard from "./leaderboard/MarketLeaderboard";
import LeaderboardHeader from "./leaderboard/LeaderboardHeader";
import LeaderboardControls from "./leaderboard/LeaderboardControls";
import ActiveFilterSummary from "./leaderboard/ActiveFilterSummary";
import {resultState,type LeaderboardModeModel} from "./leaderboard/types";
import MarketRow from "./leaderboard/MarketRow";
import ProductIdentity from "./leaderboard/ProductIdentity";
import HistoryPopover from "./leaderboard/HistoryPopover";
import FullMarketCard from "./leaderboard/FullMarketCard";

type Game=SealedGame; type Basis="market"|"median"; type View=SealedViewMode; type SortKey="name"|"signal"|"set"|"msrp"|"market"|"profit"|"profitPct";
type Product=SealedProduct; type History=PriceHistory;
const games:Record<Game,string>={pokemon:"Pokémon",onepiece:"One Piece",riftbound:"Riftbound"};
const sealedModel:LeaderboardModeModel<View,SortKey>={views:[{key:"medium",label:"Medium",icon:"▤"},{key:"text",label:"Text",icon:"☷"},{key:"full",label:"Full",icon:"▥"}],sorts:[{label:"Product",key:"name"},{label:"Set",key:"set"},{label:"MSRP",key:"msrp"},{label:"Market",key:"market"},{label:"Profit",key:"profit"},{label:"Profit %",key:"profitPct"}],viewLabel:"Sealed product view",paginationLabel:"Sealed product pages"};
const sealedSignalSort={label:"Signal",key:"signal" as SortKey};
const usd=(value:number|null)=>formatUsd(value,"N/A");
const pct=(value:number|null)=>formatPercent(value,"N/A");
const nullableSort=(a:number|null,b:number|null,direction:Direction)=>a==null?1:b==null?-1:direction==="asc"?a-b:b-a;
const typeOrder=["Booster Packs","Booster Boxes","Booster Bundles","Starter / Theme Decks","Elite Trainer Boxes","Build & Battle","Collections","Tins","Blisters","Trainer Kits / Toolkits","Cases","Boxes / Bundles","Other"];
const productType=(product:Product)=>typeOrder.includes(product.category)?product.category:"Other";
const productKey=(product:Product)=>product.productId;

export default function SealedView({signalView="leaderboard",strictness="balanced",onStrictness=()=>{},initialState,onQueryChange}:{signalView?:SignalSide;strictness?:SignalStrictness;onStrictness?:(value:SignalStrictness)=>void;initialState:SealedQueryState;onQueryChange:(state:SealedQueryState)=>void}){
 const [game,setGame]=useState<Game>(initialState.market),[query,setQuery]=useState(initialState.query),[selectedSets,setSelectedSets]=useState<string[]>(initialState.sets),[selectedTypes,setSelectedTypes]=useState<string[]>(initialState.productTypes),[marketMin,setMarketMin]=useState(initialState.marketMin),[marketMax,setMarketMax]=useState(initialState.marketMax),[msrpMin,setMsrpMin]=useState(initialState.msrpMin),[msrpMax,setMsrpMax]=useState(initialState.msrpMax),[profitMin,setProfitMin]=useState(initialState.profitMin),[profitMax,setProfitMax]=useState(initialState.profitMax),[profitPctMin,setProfitPctMin]=useState(initialState.profitPctMin),[profitPctMax,setProfitPctMax]=useState(initialState.profitPctMax),[sort,setSort]=useState<SortKey>(initialState.sort),[direction,setDirection]=useState<Direction>(initialState.direction),[perPage,setPerPage]=useState(initialState.perPage),[page,setPage]=useState(initialState.page),[view,setView]=useState<View>(initialState.view);
 const [basis,setBasis]=useState<Basis>(initialState.basis),[keepPct,setKeepPct]=useState(initialState.keepPct),[taxOn,setTaxOn]=useState(initialState.taxOn),[taxRate,setTaxRate]=useState(initialState.taxRate),[shipping,setShipping]=useState(initialState.shipping),[profitableOnly,setProfitableOnly]=useState(initialState.profitableOnly),previousSignal=useRef(signalView);
 const {items:products,status:catalogStatus,reload:reloadCatalog}=useCatalogPage({sources:[`/data/sealed-${game}.json`],parse:parseSealedProducts,keyOf:productKey});
 const {history,status:historyStatus,request:requestHistory}=usePriceHistoryBatch();
 useEffect(()=>{if(previousSignal.current===signalView)return;previousSignal.current=signalView;setSort(signalView==="leaderboard"?"profitPct":"signal");setDirection("desc");setPage(1)},[signalView]);
 useEffect(()=>onQueryChange({mode:"sealed",market:game,productTypes:selectedTypes,view,sort,direction,page,perPage,query,sets:selectedSets,marketMin,marketMax,msrpMin,msrpMax,profitMin,profitMax,profitPctMin,profitPctMax,basis,keepPct,taxOn,taxRate,shipping,profitableOnly,signal:signalView,strictness}),[game,selectedTypes,view,sort,direction,page,perPage,query,selectedSets,marketMin,marketMax,msrpMin,msrpMax,profitMin,profitMax,profitPctMin,profitPctMax,basis,keepPct,taxOn,taxRate,shipping,profitableOnly,signalView,strictness]);
 const sets=useMemo(()=>[...new Set(products.map(product=>product.set))].sort(),[products]),productTypes=useMemo(()=>[...new Set(products.map(productType))].sort((a,b)=>typeOrder.indexOf(a)-typeOrder.indexOf(b)),[products]);
 const calculate=(product:Product)=>{const value=basis==="median"?(product.midPrice??product.marketPrice):product.marketPrice,cost=product.msrp==null?null:product.msrp*(taxOn?1+taxRate/100:1)+shipping;if(value==null||cost==null)return{value,cost,proceeds:value==null?null:value*keepPct/100,profit:null,profitPct:null};const proceeds=value*keepPct/100,profit=proceeds-cost;return{value,cost,proceeds,profit,profitPct:cost?profit/cost*100:0}};
 const signalFor=(product:Product)=>signalView==="leaderboard"?null:marketSignal(history[product.productId]?.points??[],signalView,strictness,calculate(product).value);
 const filtered=useMemo(()=>{const needle=query.trim().toLowerCase();return products.filter(product=>{const result=calculate(product),value=result.value;return(!selectedSets.length||selectedSets.includes(product.set))&&(!selectedTypes.length||selectedTypes.includes(productType(product)))&&(!needle||`${product.name} ${product.set} ${product.category} ${productType(product)}`.toLowerCase().includes(needle))&&(!marketMin||value!=null&&value>=Number(marketMin))&&(!marketMax||value!=null&&value<=Number(marketMax))&&(!msrpMin||product.msrp!=null&&product.msrp>=Number(msrpMin))&&(!msrpMax||product.msrp!=null&&product.msrp<=Number(msrpMax))&&(!profitMin||result.profit!=null&&result.profit>=Number(profitMin))&&(!profitMax||result.profit!=null&&result.profit<=Number(profitMax))&&(!profitPctMin||result.profitPct!=null&&result.profitPct>=Number(profitPctMin))&&(!profitPctMax||result.profitPct!=null&&result.profitPct<=Number(profitPctMax))&&(!profitableOnly||(result.profit??-Infinity)>0)&&(signalView==="leaderboard"||Boolean(signalFor(product)))}).sort((a,b)=>{const ac=calculate(a),bc=calculate(b);if(sort==="signal")return nullableSort(signalFor(a)?.score??null,signalFor(b)?.score??null,direction);if(sort==="msrp")return nullableSort(a.msrp,b.msrp,direction);if(sort==="market")return nullableSort(ac.value,bc.value,direction);if(sort==="profit")return nullableSort(ac.profit,bc.profit,direction);if(sort==="profitPct")return nullableSort(ac.profitPct,bc.profitPct,direction);const value=sort==="name"?a.name.localeCompare(b.name):a.set.localeCompare(b.set)||a.name.localeCompare(b.name);return direction==="asc"?value:-value})},[products,selectedSets,selectedTypes,marketMin,marketMax,msrpMin,msrpMax,profitMin,profitMax,profitPctMin,profitPctMax,query,sort,direction,basis,keepPct,taxOn,taxRate,shipping,profitableOnly,history,signalView,strictness]);
 const metrics=useMemo(()=>{const rows=filtered.map(calculate),priced=rows.filter(row=>row.value!=null);return{market:priced.reduce((sum,row)=>sum+row.value!,0),count:rows.length}},[filtered,basis,keepPct,taxOn,taxRate,shipping]);
 const pages=Math.max(1,Math.ceil(filtered.length/perPage)),visible=filtered.slice((page-1)*perPage,page*perPage);
 useEffect(()=>{const targets=signalView==="leaderboard"?visible:products.slice(0,400);void requestHistory(targets.map(product=>({productId:product.productId,printing:"Normal",sealed:true})))},[(signalView==="leaderboard"?visible:products.slice(0,400)).map(product=>product.productId).join(","),requestHistory]);
 const changeSort=(next:SortKey)=>{setDirection(current=>sort===next?(current==="asc"?"desc":"asc"):(next==="name"||next==="set"?"asc":"desc"));setSort(next);setPage(1)};
 const rowDetails=(product:Product,result:ReturnType<typeof calculate>,h?:History,large=false)=>{const movement=(label:string,value:number|null|undefined)=>({label,value:value===undefined?"…":pct(value??null),tone:movementTone(value)});return <HistoryPanel title={h?.condition??"Sealed market history"} subtitle={h?.variant??product.category} points={h?.points??[]} label="sealed market" metrics={[{label:"MSRP",value:usd(product.msrp)},{label:basis,value:usd(result.value)},{label:"30D low",value:usd(h?.low30??null)},{label:"30D high",value:usd(h?.high30??null)},{label:"Hist low",value:usd(h?.historyLow??null)},{label:"Profit",value:usd(result.profit),tone:movementTone(result.profit)},{label:"Profit %",value:pct(result.profitPct),tone:movementTone(result.profitPct)},movement("7 day",h?.change7),movement("30 day",h?.change30),movement("90 day",h?.change90)]} large={large}/>};
 const activeSorts=signalView==="leaderboard"?sealedModel.sorts:[sealedModel.sorts[0],sealedSignalSort,...sealedModel.sorts.slice(1)];
 const summaryFilters=[query?{key:"query",label:`Search: ${query}`,clear:()=>setQuery("")}:null,selectedTypes.length?{key:"types",label:`${selectedTypes.length} Product Type${selectedTypes.length===1?"":"s"}`,clear:()=>setSelectedTypes([])}:null,selectedSets.length?{key:"sets",label:`${selectedSets.length} Set${selectedSets.length===1?"":"s"}`,clear:()=>setSelectedSets([])}:null,marketMin||marketMax?{key:"market",label:`Market: ${marketMin||"Any"}–${marketMax||"Any"}`,clear:()=>{setMarketMin("");setMarketMax("")}}:null,msrpMin||msrpMax?{key:"msrp",label:`MSRP: ${msrpMin||"Any"}–${msrpMax||"Any"}`,clear:()=>{setMsrpMin("");setMsrpMax("")}}:null,profitMin||profitMax?{key:"profit",label:`Profit: ${profitMin||"Any"}–${profitMax||"Any"}`,clear:()=>{setProfitMin("");setProfitMax("")}}:null,profitPctMin||profitPctMax?{key:"profitPct",label:`Profit %: ${profitPctMin||"Any"}–${profitPctMax||"Any"}`,clear:()=>{setProfitPctMin("");setProfitPctMax("")}}:null,profitableOnly?{key:"profitable",label:"Profitable Only",clear:()=>setProfitableOnly(false)}:null].filter((item):item is {key:string;label:string;clear:()=>void}=>Boolean(item));
 return <MarketLeaderboard className="sealed-market" id="sealed-market" historyStatus={historyStatus} marketStrip={<section className="market-strip sealed-market-strip"><label><span>Market</span><select aria-label="Sealed market" value={game} onChange={event=>{setGame(event.target.value as Game);setSelectedSets([]);setSelectedTypes([]);setPage(1)}}><option value="pokemon">Pokémon</option><option value="onepiece">One Piece</option><option value="riftbound">Riftbound</option></select></label><MultiSelectField label="Product type" options={productTypes.map(type=>({key:type,label:type}))} selected={selectedTypes} onChange={values=>{setSelectedTypes(values);setPage(1)}} allLabel="All product types" searchable={false}/><div><span>Products available</span><strong>{metrics.count.toLocaleString()}</strong><small>{usd(metrics.market)} combined market</small></div><label><span>Products per page</span><select aria-label="Sealed products per page" value={perPage} onChange={event=>{setPerPage(Number(event.target.value));setPage(1)}}><option>20</option><option>30</option><option>40</option><option>50</option></select></label></section>} header={<LeaderboardHeader className="sealed-summary" kicker="Sealed product intelligence" kickerClassName="kicker" title={`${games[game]} ${signalView==="leaderboard"?"Sealed":signalView==="buy"?"Hot Buys":"Hot Sells"}`} description={signalView==="leaderboard"?"Verified MSRP compared with current TCGplayer pricing.":`${strictness[0].toUpperCase()+strictness.slice(1)} signals ranked by proximity, volatility, and data confidence.`} summary={<ActiveFilterSummary items={summaryFilters} matches={filtered.length} label="Active sealed filters" signalLabel={signalView!=="leaderboard"?`${strictness[0].toUpperCase()+strictness.slice(1)} Signals`:undefined} onRemove={()=>setPage(1)}/>} aside={<div className="price-basis" aria-label="Price basis"><i aria-hidden="true"/><button className={basis==="market"?"active":""} onClick={()=>setBasis("market")}>Market</button><button className={basis==="median"?"active":""} onClick={()=>setBasis("median")}>Median</button></div>}/>} beforeControls={<SaleScenario keepPct={keepPct} onKeepPct={setKeepPct} taxOn={taxOn} onTaxOn={setTaxOn} taxRate={taxRate} onTaxRate={setTaxRate} shipping={shipping} onShipping={setShipping}/>} controls={<LeaderboardControls className={`sealed-toolbar ${signalView!=="leaderboard"?"has-strictness":""}`}><label className="sealed-toolbar-search"><span>⌕</span><input value={query} onChange={event=>{setQuery(event.target.value);setPage(1)}} placeholder="Search product, set, or type"/></label>{signalView!=="leaderboard"&&<StrictnessControl value={strictness} onChange={value=>{onStrictness(value);setPage(1)}}/>}<SealedFilters sets={sets} selectedSets={selectedSets} onSets={values=>{setSelectedSets(values);setPage(1)}} marketMin={marketMin} marketMax={marketMax} onMarketMin={value=>{setMarketMin(value);setPage(1)}} onMarketMax={value=>{setMarketMax(value);setPage(1)}} msrpMin={msrpMin} msrpMax={msrpMax} onMsrpMin={value=>{setMsrpMin(value);setPage(1)}} onMsrpMax={value=>{setMsrpMax(value);setPage(1)}} profitMin={profitMin} profitMax={profitMax} onProfitMin={value=>{setProfitMin(value);setPage(1)}} onProfitMax={value=>{setProfitMax(value);setPage(1)}} profitPctMin={profitPctMin} profitPctMax={profitPctMax} onProfitPctMin={value=>{setProfitPctMin(value);setPage(1)}} onProfitPctMax={value=>{setProfitPctMax(value);setPage(1)}} profitableOnly={profitableOnly} onProfitableOnly={value=>{setProfitableOnly(value);setPage(1)}} onReset={()=>{setSelectedSets([]);setMarketMin("");setMarketMax("");setMsrpMin("");setMsrpMax("");setProfitMin("");setProfitMax("");setProfitPctMin("");setProfitPctMax("");setProfitableOnly(false);setPage(1)}}/><SegmentedView value={view} options={sealedModel.views} label={sealedModel.viewLabel} onChange={next=>{setView(next);setPage(1)}}/></LeaderboardControls>} sortSurface={view==="full"?<SortToolbar items={activeSorts.map(item=>item.key==="market"?{...item,label:basis==="market"?"Market":"Median"}:item)} sort={sort} direction={direction} onSort={changeSort} label="Full sealed product sorting"/>:<div className={`sealed-head ${signalView!=="leaderboard"?"has-signal":""}`}>{activeSorts.map(item=><SortableHeader key={item.key} label={item.key==="market"?(basis==="market"?"Market":"Median"):item.label} column={item.key} sort={sort} direction={direction} onSort={changeSort}/>)}</div>} rowsClassName={`sealed-rows sealed-view-${view}`} state={resultState(catalogStatus,!visible.length)} loadingLabel="Loading sealed products" skeletonCount={8} errorMessage="Sealed-product data is temporarily unavailable." emptyMessage="No sealed products match the current selection." onRetry={reloadCatalog} rows={visible.map(product=>{const result=calculate(product),h=history[product.productId],priced=result.value!=null,comparable=result.profit!=null,signal=signalFor(product),columnSignal=signal&&(view==="medium"||view==="text");if(view==="full")return <span className="signal-card-wrap" key={product.productId}>{signal&&<SignalBadge signal={signal}/>}<FullMarketCard className="sealed-full-card" artClassName="sealed-full-art" dataClassName="sealed-full-data" titleClassName="sealed-full-title" image={product.image} alt={`${product.name} product`} title={product.name} meta={<>{product.set} · {product.category}</>} content={rowDetails(product,result,h)}/></span>;return <MarketRow className={`sealed-row ${columnSignal?"has-signal":""}`} key={product.productId} label={`Show ${product.name} price history`} popupWidth={650} popover={<HistoryPopover className="sealed-hover-card" identityClassName="sealed-hover-identity" artClassName="sealed-hover-art" image={product.image} alt={`${product.name} product`} badge={signal&&<SignalBadge signal={signal}/>} label={`${product.name} price history`}>{rowDetails(product,result,h)}</HistoryPopover>}><ProductIdentity className="sealed-product" image={product.image} alt="" title={product.name} meta={product.category}/>{columnSignal&&<span className="sealed-signal-cell"><SignalBadge signal={signal!}/></span>}<span className="sealed-category">{product.set}</span><span data-label="MSRP"><b>{usd(product.msrp)}</b><small>{product.msrp==null?"MSRP unavailable":taxOn?`Cost ${usd(result.cost)}`:product.msrpSource}</small></span><span data-label={basis==="market"?"Market":"Median"}><b>{usd(result.value)}</b><small>{priced?(keepPct<100?`Proceeds ${usd(result.proceeds)}`:"TCGplayer value"):"No TCGplayer data"}</small></span><span data-label="Profit" className={!comparable?"":result.profit!>=0?"profit-positive":"profit-negative"}><b>{!comparable?"N/A":`${result.profit!>=0?"+":""}${usd(result.profit)}`}</b></span><span data-label="Profit %"><b className={`profit-pill ${!comparable?"unavailable":result.profitPct!>=0?"positive":"negative"}`}>{!comparable?"N/A":`${result.profitPct!>=0?"+":""}${result.profitPct!.toFixed(1)}%`}</b></span></MarketRow>})} pagination={{page,pages,onChange:setPage,label:sealedModel.paginationLabel}} footer={<p className="sealed-note">Profit uses the selected price basis and assumptions above. Defaults compare TCGplayer market directly with published US MSRP; fees, tax, and shipping are optional scenario inputs, not predictions.</p>}/>;
}
